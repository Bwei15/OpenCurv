// Standalone JVM build used to compile and test the framework-free core of
// OpenCurv (routing, geodesy, filtering, the turn-by-turn state machine)
// without needing the Android SDK. See tools/verifier/README.md.
rootProject.name = "opencurv-verifier"
