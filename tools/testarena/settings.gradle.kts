// Standalone JVM build for the OpenCurv "Testarena": a synthetic OSM map
// generator plus a route-scoring harness. Deliberately NOT part of the
// Android build (see tools/testarena/README.md) - mirrors how
// tools/verifier keeps the routing-logic tests independent of the Android
// SDK.
rootProject.name = "opencurv-testarena"
